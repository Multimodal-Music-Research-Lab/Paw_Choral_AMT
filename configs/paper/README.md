# Paper configuration status

Exact expanded configurations for the reported manuscript rows have not yet
been recovered. Do not place inferred YAML files here and label them as exact.

For every recovered run, add a resolved, immutable configuration named after
the system (`pagct.yaml`, `pawct_no_union.yaml`, `pawct_union.yaml`,
`pawct_rp.yaml`, `pawct_rp_oc.yaml`, and `post_va.yaml`) together with
checkpoint and split hashes. Until then,
`src/config.yaml` is an executable example rather than the paper record.
